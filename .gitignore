node_modules
.next
out
# env: ignore everything (public repo) but allow a committed example if added later
.env
.env*.local
.env.development
.env.production
!.env.example
# TypeScript incremental build cache
tsconfig.tsbuildinfo
.DS_Store
*.log
# Playwright artifacts (screenshots ARE committed intentionally)
test-results/
playwright-report/
